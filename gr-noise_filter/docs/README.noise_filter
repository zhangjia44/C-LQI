This is the noise_filter-write-a-block package meant as a guide to building
out-of-tree packages. To use the noise_filter blocks, the Python namespaces
is in 'noise_filter', which is imported as:

    import noise_filter

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(noise_filter)
